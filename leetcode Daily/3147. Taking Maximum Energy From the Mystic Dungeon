  int maximumEnergy(vector<int>& energy, int k) {
   int n=energy.size(),m=INT_MIN;
    for(int i=n-1;i>=0;i--){
        if(i+k<n) {
        energy[i]+=energy[i+k];
        }
        m=max(m,energy[i]);
    }
    return m;
    }
